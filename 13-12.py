# Define the function print_square_number to receive an argument `n`
def print_square_number(n):
# Pass a value to argument `n` at the time of call and print on screen “the square of `n`: the value of `n` squared ”
    print("%s squared:" % (n),n*n)

print_square_number(2)
print_square_number(3)
